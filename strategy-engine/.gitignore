# Strategy output / log dirs (generated at runtime)
output/
__pycache__/
*.log
*.csv
*.txt
*.local

# runtime log dirs the live scripts create next to themselves / in CWD
bridge/
Live/bridge/

# backtest replay output (one dir per run)
runs/

# fleet-wide strategy config: Telegram credentials and the trade DB URL.
# Deployed to the strategy host, never committed — Version 2 moved these out
# of the per-strategy JSONs so a live database password stops living in git.
Live/engine.json
