apiVersion: apps/v1
kind: Deployment
metadata:
  name: s17-m3m2-v1-btcusd-sell
  namespace: trading
spec:
  replicas: 1
  selector:
    matchLabels:
      app: s17-m3m2-v1-btcusd-sell
      component: strategy
  template:
    metadata:
      labels:
        app: s17-m3m2-v1-btcusd-sell
        component: strategy
    spec:
      containers:
      - name: strategy
        image: 561789488706.dkr.ecr.ap-south-1.amazonaws.com/strategy-engine:latest
        env:
        - name: STRATEGY_SCRIPT
          value: "Live/Strategy 17/s17_m3m2_v1_btcusd_sell.py"
        - name: ENGINE_CONFIG_JSON
          valueFrom:
            secretKeyRef:
              name: engine-config
              key: json
        resources:
          requests:
            memory: "256Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          exec:
            command:
            - /bin/sh
            - -c
            - "[ $(( $(date +%s) - $(stat -c %Y /tmp/heartbeat 2>/dev/null || echo 0) )) -lt 180 ]"
          initialDelaySeconds: 120
          periodSeconds: 30
          failureThreshold: 3
        readinessProbe:
          exec:
            command: ["/bin/sh", "-c", "test -f /tmp/heartbeat"]
          initialDelaySeconds: 90
          periodSeconds: 15

