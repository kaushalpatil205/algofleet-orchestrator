apiVersion: v1
kind: Service
metadata:
  name: trade-dashboard
  namespace: trading
spec:
  selector:
    app: trade-dashboard
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
  type: ClusterIP
