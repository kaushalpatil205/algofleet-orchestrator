apiVersion: v1
kind: Service
metadata:
  name: trade-dashboard
  namespace: trading
  annotations:
    service.beta.kubernetes.io/aws-load-balancer-type: nlb
spec:
  selector:
    app: trade-dashboard
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
  type: LoadBalancer
